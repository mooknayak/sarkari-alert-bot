[project]
name = "sarkari-alert-bot-api"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "requests",
    "beautifulsoup4",
    "PyMuPDF",
    "Pillow",
    "feedparser",
]

[tool.vercel]
entrypoint = "telegram_webhook:handler"
